CREATE TABLE recipes (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    description TEXT
);

-- Example query so SQLDelight generates an interface
selectAll:
SELECT * FROM recipes;

insertRecipe:
INSERT INTO recipes(title, description)
VALUES (?, ?);
